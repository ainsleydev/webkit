---
'@ainsleydev/sveltekit-helper': patch
---

Fix icon colour CSS custom properties (`--_alert-icon-colour`, `--_notice-icon-colour`) not being applied to icons in Alert and Notice components. Add `hideIcon` prop to both components to optionally hide the icon.

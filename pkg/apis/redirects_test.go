package apis

import "testing"

func TestRedirects(t *testing.T) {
	t.Parallel()

	tt := map[string]struct {
	}{
		"No": {},
	}
	for name, test := range tt {
		t.Run(name, func(t *testing.T) {

		})
	}
}
